fastapi>=0.110
uvicorn[standard]>=0.29
PyJWT>=2.8
google-auth>=2.28
# google.auth.transport.requests needs the `requests` package at import time;
# without it `dashboard.backend.auth` fails to import and the app won't start.
requests>=2.31
pydantic>=2.6

# Optional — only needed for the Claude narrative generator
# (dashboard/narrative/). The read-only dashboard runs without these.
anthropic>=0.40
jinja2>=3.1
