"""agent-evalkit — an evaluation framework for AI agents that produce files
and call tools. The Adapter seam (evalkit.adapter) is the one abstraction
you implement; the rest (loader / judge / scorer / leaderboard / pipeline)
is adapter-agnostic."""

__version__ = "0.1.0"
